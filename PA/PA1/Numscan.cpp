#include <iostream>
#include <Numscan.h>
using std::cout;
using std::cin;
using std::endl;
#include <fstream>
using std::ifstream;
using std::cerr;
/**
int scanFile(istream& sfile){
	int tmp;
	while(!sfile.fail()){
		sfile >> tmp;
		
		if(!sfile.fail() && tmp>=0 && tmp<20){
			//break;
			this.numbers[tmp]=tmp;
		} else{
				//return Usage(argv[0]);
		}
		//ns.addNumber(tmp);
	}
	
	return 0;
}
//*/
/**
int printNums(){
	try{
	for(int i=0; i<20;i++){
		cout<<i<<" "<<this.numbers[i]<<endl;
		
		
	}
	return 0;
	}catch(const std::exception& e){
		cerr<<"Failed printing the loop"<<endl;
		return -1;}
	return -1;
};//*/

